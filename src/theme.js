import {createMuiTheme} from "@material-ui/core";

export const theme = createMuiTheme({
  palette: {
    primary: {
      light: `#A783CB`,
      main: `#734F96`,
      dark: `#3E1A62`
    },
    secondary: {
      light: `#EFE993`,
      main: `#DED86D`,
      dark: `#908A23`
    },
    grey: {
      main: ``
    }
  }
})
